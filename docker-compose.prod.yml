services:
  sttm-admin:
    container_name: sttm-admin-prod
    image: ghcr.io/${GITHUB_REPOSITORY}:latest
    restart: unless-stopped
    build:
      context: .
      dockerfile: Dockerfile.prod
    ports:
      - "5173:80"
    networks:
      - prod

networks:
  prod:
