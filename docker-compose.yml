services:
  sttm-admin:
    container_name: sttm-admin-dev
    restart: unless-stopped
    build:
      context: .
      dockerfile: Dockerfile
    parts:
      - "5173:5173"
    volumes:
      - .:/app
      - /app/node_modules:/app/node_modules
    networks:
      - dev

networks:
  dev: